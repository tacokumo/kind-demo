apiVersion: v1
kind: Service
metadata:
  name: valkey
  namespace: tacokumo-admin
  labels:
    app: valkey
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
  selector:
    app: valkey
